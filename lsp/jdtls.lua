-- TODO: jdtls requires per-workspace data directories and JVM args.
-- This config is aspirational — fill in cmd, filetypes, root_markers,
-- and workspace-specific settings before enabling.
return {
	cmd = { "jdtls" },
	settings = {
		java = {},
	},
}
